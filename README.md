# Environment configuration

Presumes that this repo is cloned to the home folder:
`~/.config/README.md` would be this file

### Requires symlinks
```bash
~/.gitconfig -> ~/.config/git/.gitconfig
~/.zshrc -> ~/.config/zsh/.zshrc
```

### Required Environment vars
```bash

```

